# 更新日志

## 从 GitHub 旧公开版到当前版本的主要变化

这份日志用于说明仓库中旧公开版与当前版本之间的差异，以及本次版本整理后的优化点。

## 1. 仓库结构变化

旧公开版仓库特点：

- 公开仓库中直接放了 4 个浏览器驱动文件
- 仓库内容较少，主要以 `mooc_robot.py` 为主
- 使用说明较简短

当前版本仓库特点：

- 移除了浏览器驱动二进制，不再公开分发
- 新增 `.gitignore`，避免提交本地敏感配置和构建产物
- 补充 `README.md`，形成完整使用说明
- 新增 `CHANGELOG.md`，集中说明版本差异
- 保留 `package_exe.py`、`build.py`、`mooc_robot.spec`，方便后续继续打包 EXE

## 2. 题目抓取能力升级

旧版：

- 更偏向整页文本抓取
- 对复杂题型和结构化处理支持有限

当前版本：

- 改为按题目块结构化提取
- 支持提取：
  - `qid`
  - 题号
  - 题型
  - 题干
  - 选项
  - 填空数量
  - 可写入字段
- 对选择题、填空题、判断题、论述题的抓取更完整
- 减少了编辑器工具栏噪音文本对题干识别的干扰

## 3. AI 解题流程优化

旧版：

- AI 返回更偏自然语言结果
- 自动化回填能力有限

当前版本：

- AI 提示词升级为结构化题目输入
- AI 输出支持“JSON 答案块 + 说明文字”
- 增加结构化答案解析与校验
- 对题型、选项范围、填空数量、缺失题目等做了更严格校验
- 当 JSON 格式不标准时，支持自动重排修复

## 4. 自动填写能力增强

旧版：

- 自动化能力较基础
- 对不同题型的统一回填支持不完整

当前版本：

- 按 `qid` 精准定位题目
- 单选 / 判断题支持原生点击写入
- 多选题支持按目标集合自动点击和校验
- 填空题支持按空位顺序写入
- 论述题 / 文本题支持 UEditor / textarea 写入
- 自动填写后生成成功 / 失败 / 跳过汇总
- 默认只填写，不自动提交

## 5. 论述题稳定性修复

旧版问题：

- 论述题在页面里实际已经填写成功
- 程序仍可能误判为“填写失败”

当前版本修复：

- 对文本题改为读取 UEditor 实际内容状态进行校验
- 增加短轮询等待页面同步
- 同时参考编辑器内容、textarea 内容和答题卡状态
- 降低“已写入但误报失败”的概率

## 6. 控制台与运行体验优化

旧版：

- 输出较原始
- Chromium 相关底层日志更容易直接刷到控制台

当前版本：

- CLI 输出改为更清晰的轻量卡片式风格
- 状态提示统一为 `[OK]`、`[WARN]`、`[ERR]`、`[INFO]`
- 主菜单新增“重新检测浏览器驱动”
- 静默了 Chromium 的 SSL handshake 噪音日志

## 7. 打包与分发方式优化

旧版：

- 仓库和打包形态没有明显区分“源码运行”和“交付运行”

当前版本：

- 明确支持单文件 EXE 打包
- 采用“EXE + 外置驱动 + 外置配置”的桌面交付方式
- 浏览器驱动不打进 EXE
- 配置文件不打进 EXE
- EXE 运行时读写行为与 `.py` 版本保持一致

## 8. 公开仓库安全性优化

本次仓库整理重点之一是公开发布安全：

- 不再把浏览器驱动放到公开仓库
- 不上传真实 `api.txt`
- 不上传真实 `page_cookie.txt`
- 不上传真实 `page_address.txt`
- 用 `.gitignore` 明确排除敏感文件和构建产物

## 9. 总结

相较旧公开版，当前版本的提升重点主要体现在：

- 题目抓取更完整
- AI 输出更结构化
- 自动填写题型覆盖更广
- 论述题稳定性更好
- 终端交互更清晰
- 打包交付更规范
- 公开仓库更安全
